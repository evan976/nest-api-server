export enum PublishState {
  Publish = 1,
  Draft = 0,
}

export enum OriginState {
  Original = 0,
  Reprint = 1,
  Hybrid = 2,
}

export enum CommentState {
  Recycle = -1,
  Reject = 0,
  Pass = 1,
}

export enum Weights {
  Large = 3,
  Medium = 2,
  Small = 1,
}
