export enum PublishState {
  Publish = 1,
  Draft = 0
}

export enum OnlineState {
  Online = 1,
  Offline = 0
}

export enum OriginState {
  Original = 0,
  Reprint = 1,
  Hybrid = 2
}

export enum CommentState {
  Recycle = -1,
  Review = 0,
  Pass = 1
}
